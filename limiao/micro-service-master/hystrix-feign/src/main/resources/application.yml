server:
  port: 7079
spring:
  application:
    name: feign-hystrix
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8100/eureka/
  instance:
    hostname: feign
ribbon:
  eureka:
    enabled: true
    