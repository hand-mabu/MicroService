server:
  port: 1112
info:
  test1:
    name: ling
    email: 18483690930@163.com
endpoints:
  mappings:
    sensitive: false
management:
  security:
    enabled: false
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      #    注册服务中心的地址模板
      defaultZone: http://${eureka.instance.hostname}:1111/eureka/
